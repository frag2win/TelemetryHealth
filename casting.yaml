version: 1
name: telemetryhealth-signoz
target: kubernetes
flavor: kustomize
variables:
  signoz:
    version: "0.112.0"
  clickhouse:
    mode: single-node
  pipeline:
    receivers:
      otlp:
        enabled: true
    processors:
      telemetryhealth:
        enabled: true
